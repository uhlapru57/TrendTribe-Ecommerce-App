import 'package:flutter/material.dart';

class KidsProducts extends StatelessWidget {
  const KidsProducts({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('No Content added here'));
  }
}
